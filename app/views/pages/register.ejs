<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <%- include('../partials/seo') %>
  <link rel="icon" type="image/x-icon" href="/images/favicon.ico">
  <link rel="stylesheet" href="/css/forms.css">
  <link href="https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css" rel="stylesheet">
  <style>
    .error-message { color: #ff4d4d; font-size: 0.9rem; margin-top: 4px; display:block; }
    .success-message { color: #4CAF50; font-size: 1rem; margin-bottom: 10px; display:block; text-align:center; }
  </style>
</head>
<body>
  <section class="wrapper">
    <form id="registerForm" autocomplete="off">
      <% if (typeof user !== 'undefined' && user) { %>
      <section style="background:#1a1a1a;border-left:4px solid #C98B1D;color:#ccc;padding:10px 14px;font-size:.85rem;margin-bottom:14px;border-radius:6px;">
        <strong style="color:#C98B1D;">Você está logado como <%= user.nome %>.</strong><br>
        Criar uma nova conta não afetará sua sessão atual.
      </section>
      <% } %>
      <h1>Registrar</h1>
      <span class="success-message"></span>

      <section class="input-box">
        <input type="text" id="nome" name="nome" placeholder="Nome completo" autocomplete="off">
        <i class='bx bxs-user'></i>
        <span class="error-message" id="nome-error"></span>
      </section>

      <section class="input-box">
        <input type="text" id="cpf" name="cpf" placeholder="CPF (somente números)" autocomplete="off">
        <i class='bx bxs-id-card'></i>
        <span class="error-message" id="cpf-error"></span>
      </section>

      <section class="input-box">
        <input type="email" id="email" name="email" placeholder="E-mail" autocomplete="off">
        <i class='bx bx-envelope'></i>
        <span class="error-message" id="email-error"></span>
      </section>

      <section class="input-box">
        <input type="text" id="cep" name="cep" placeholder="CEP">
        <i class='bx bxs-map'></i>
        <span class="error-message" id="cep-error"></span>
      </section>

      <section class="input-box">
        <input type="password" id="password" name="password" placeholder="Senha">
        <button type="button" class="toggle-password" data-target="password" title="Mostrar/ocultar senha">
          <i class='bx bx-hide'></i>
        </button>
        <span class="error-message" id="password-error"></span>
      </section>

      <section class="input-box">
        <input type="password" id="confirmPassword" name="confirmPassword" placeholder="Confirmar senha">
        <button type="button" class="toggle-password" data-target="confirmPassword" title="Mostrar/ocultar senha">
          <i class='bx bx-hide'></i>
        </button>
        <span class="error-message" id="confirmPassword-error"></span>
      </section>

      <section class="remember-forgot">
        <label><input type="checkbox" id="terms" name="terms"> Aceito os termos de uso</label>
        <span class="error-message" id="terms-error"></span>
      </section>

      <button type="submit" class="btn">Registrar</button>

      <section class="register-link">
        <p>Já tem uma conta? <a href="/login">Entrar</a></p>
      </section>
    </form>

    <a href="/"><img src="/images/logo-form.png" class="logo" alt="Logo GymBros"></a>
  </section>

  <script>window.GYMBROS_ALREADY_LOGGED_IN = <%= (typeof user !== 'undefined' && user) ? 'true' : 'false' %>;</script>
  <script src="/js/forms.js" type="module"></script>
</body>
</html>
